# TuteDude_Assignment---2

Task - 1

First of all input the number using the input function.
then create a condition statement:
if the number % 2 == 0, then the number is even.
else, the number % 2 != 0, then number is odd.


Task - 2

total = 0, because store the sum of all number in variable.
then create the conditional statement:
in the conditional statement we have get the range 1 to 50 numbers.
after get the number add for all numbers in range.
